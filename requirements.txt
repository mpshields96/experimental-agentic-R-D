# Titanium-Agentic — Dependencies
# Last updated: 2026-02-18 (Session 1)
# Install: pip install -r requirements.txt (inside sandbox venv only)

# Core
streamlit>=1.54.0
requests>=2.31.0
plotly>=5.18.0
pandas>=2.0.0

# Scheduling (in-process, no Celery overhead)
APScheduler>=3.10.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Database (stdlib — no install needed)
# sqlite3 is part of Python standard library

# Optional: for development
# python-dotenv>=1.0.0  # load .env for local dev (never commit .env)
